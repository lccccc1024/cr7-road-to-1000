# 项目审计记录

日期：2026-09-10

## 当前结论

经过多轮代码审查、修复和回归验证，当前未发现已知的技术上线阻断问题。本记录对应上线前验证，部署结果以 GitHub Actions 和实际线上检查为准。

## 本轮发现与修复

- 数据文件解析原先会被字符串中的 `};` 提前截断，导致后续同步失败。改为解析完整声明中的 JSON，新增特殊字符和拒绝附加代码的测试。
- 添加发布资源存在性测试，检查页面引用的本地资源及工作流中的 assets 打包步骤。
- 保留彩色人物照片，提升小字可读性和移动端触控区域。
- 确认线上 Pages 使用旧的分支发布方式，发布时需切换为 workflow，保留 `cr7.950922.xyz` 自定义域名。

## 本轮验证

- `node --test`：17/17 通过。
- `node sync-goals.js --dry-run`：CSV 共 978 行，与来源总数匹配，仓库数据无需更新。
- JavaScript 语法及 Git 差异检查通过。
- Chrome 实测 320、375、760、768、1024、1280px，包含长对手名称和本地记录状态，未发现横向溢出或脚本错误。
- 主图加载成功，滤镜为 none、混合模式为 normal；1000 球显示已达成并隐藏预计时间。
- 在沙箱外成功验证系统 GitHub 登录，远程 main 与本地基线一致。此前“凭据无效”提示来自沙箱无法读取系统凭据，并非实际账户失效。

## 既有回归覆盖

千球边界、总数不变时修复明细、源异常不写入、主源滞后时备用源回退、CSV 引号/换行/BOM、缺失字段及非法日期、缓存更新与损坏、禁用存储、本地日期、数据结构一致性、浏览器校验不改写数据。

## 验证边界

第三方统计源的历史 CSV 存在内容或口径异常。结构测试不能证明赛事事实准确；页面已标注非官方来源，历史里程碑保持独立核对。未发现已知问题不等于绝对无缺陷，首次生产部署仍需确认发布成功并检查线上资源。

部署阶段跟进：GitHub 提示 checkout/setup-node v4 使用的 Node 20 运行时已弃用，已升级为 v6（Node 24 action runtime）；项目测试运行环境仍为 Node 22。Pages 已切换为 workflow，原域名保留。
